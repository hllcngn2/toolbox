#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#include "rngbrush.h"

int	main(int ac, char **av){
srand((unsigned int)time(NULL));

printf("hello ra");
printf("\ngenerated: %c %c %c %c", ra(), ra(), ra(), ra());
printf("\ngenerated: %c %c %c %c", ra(), ra(), ra(), ra());
printf("\ngenerated: %c %c %c %c", ra(), ra(), ra(), ra());
printf("\ngenerated: %c %c %c %c", ra(), ra(), ra(), ra());
printf("\nend test");

return 0;}
